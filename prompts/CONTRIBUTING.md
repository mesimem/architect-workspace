# Contributing to the prompt library

Copy `_template/` to a new folder named after your prompt, fill in every header field, and add at least one eval line before marking the prompt `status: active`.
